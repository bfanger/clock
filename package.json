{
  "name": "clock",
  "version": "4.0.0",
  "private": true,
  "scripts": {
    "build": "go install ./cmd/clock",
    "dev": "gow -c run ./cmd/clock",
    "dev:race": "gow -c run -race ./cmd/clock",
    "garbage-truck": "gow -c run ./cmd/garbage-truck",
    "school-schedule": "gow -c run ./cmd/school-schedule",
    "reminders": "gow -c run ./cmd/reminders",
    "weather": "gow -c run ./cmd/weather",
    "test": "go test ./pkg/tween ./internal/app ./internal/schedule"
  }
}
